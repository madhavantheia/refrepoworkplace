package org.facebook.products.steps;

import io.cucumber.java.en.Given;

public class GainEntry {
    @Given("I go to the {string} page")
    public void i_want_to_open_page(String webpage) {
        // webpageFactory.openPage(webpage); "So, webpages can be added as objects too and can be accessed using the Objects
        // Also write the implementation code right in the step definitions class, that's what they actually are there for
    }
}
